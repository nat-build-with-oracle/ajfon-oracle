# บทที่ 4 — Cosine Similarity: สมการเดียวที่ต้องรู้

> ภาค 2 เริ่มที่นี่ — คุณเห็นระบบทำงานมา 3 บทแล้ว ตอนนี้ถึงเวลาเปิดฝาดูข้างใน
> ข่าวดี: ข้างในมีสมการหลักแค่ **บรรทัดเดียว**

---

## 4.1 ทวนภาพใหญ่ก่อน

บทที่ 1-3 เราเห็นว่า: ข้อความ → ตัวเลขหลายมิติ (embedding) → หา "ตัวที่ใกล้ที่สุด"
คำถามเดียวที่ค้างอยู่คือ **"ใกล้" วัดยังไง?** — คำตอบคือ cosine similarity:

$$\cos(A, B) = \frac{A \cdot B}{\|A\| \, \|B\|} = \frac{\sum_i a_i b_i}{\sqrt{\sum_i a_i^2} \sqrt{\sum_i b_i^2}}$$

- **เศษ** (dot product): จับคู่ตัวเลขตำแหน่งเดียวกัน คูณกัน แล้วบวกรวม — ทิศเดียวกันได้ค่าบวกมาก
- **ส่วน** (ขนาดของเวกเตอร์ทั้งสอง): normalize ให้ผลไม่ขึ้นกับ "ความยาว" ของเวกเตอร์
- ผลลัพธ์: **-1 ถึง 1** — ยิ่งใกล้ 1 ทิศยิ่งตรงกัน = ความหมายยิ่งใกล้

## 4.2 คำนวณมือบนกระดาษ (2 มิติ)

สมมติโลกมีแค่ 2 แกนความหมาย: `[ความเป็นแมว, ความเป็นรถ]`

| คำ | เวกเตอร์ |
|----|----------|
| แมว | [0.9, 0.1] |
| ลูกแมว | [0.85, 0.15] |
| รถยนต์ | [0.1, 0.95] |

คิด cos(แมว, ลูกแมว) ทีละขั้น:

```
dot   = 0.9×0.85 + 0.1×0.15        = 0.78
|แมว|  = √(0.9² + 0.1²)             = 0.906
|ลูกแมว| = √(0.85² + 0.15²)          = 0.863
cos   = 0.78 / (0.906 × 0.863)     = 0.998   ← เกือบ 1: ความหมายแทบเดียวกัน
```

ส่วน cos(แมว, รถยนต์) = **0.214** — ทิศเกือบตั้งฉาก = คนละเรื่อง

## 4.3 โค้ด 5 บรรทัด (ทั้งฟังก์ชัน)

```python
import math

def cosine(a, b):
    dot = sum(x * y for x, y in zip(a, b))
    na  = math.sqrt(sum(x * x for x in a))
    nb  = math.sqrt(sum(x * x for x in b))
    return dot / (na * nb)
```

ไม่มีเวทมนตร์ ไม่มี AI ในฟังก์ชันนี้ — แค่คูณ บวก หาร

## 4.4 สเกลขึ้นของจริง: 1024 มิติ ฟังก์ชันเดิมเป๊ะ

demo4 ใช้ **ฟังก์ชัน cosine ตัวเดียวกันข้างบน** กับ embedding จริงจาก bge-m3 (ผลรันจริง):

```
มิติจริง: 1024
cos("แมวนอนบนโซฟา", "ลูกแมวหลับอยู่บนเก้าอี้") = 0.8181
cos("แมวนอนบนโซฟา", "ราคาน้ำมันดีเซลปรับขึ้น")  = 0.3065
```

จุดที่ควรหยุดคิด: **สองประโยคแมวไม่มีคำซ้ำกันเลยสักคำ** (แมว/ลูกแมว, โซฟา/เก้าอี้, นอน/หลับ)
แต่ cosine = 0.82 — เพราะ bge-m3 แปลงให้ประโยคความหมายใกล้กันชี้ทิศใกล้กัน (บทที่ 5: มันทำได้ยังไง)

## 4.5 แล้ว vector database ทำอะไรกันแน่?

ตอนนี้ตอบได้เต็มปาก:

> **ChromaDB (และ vector DB ทุกตัว) = ที่เก็บเวกเตอร์ + เครื่องหา cosine สูงสุดแบบเร็วมาก**

`col.query(...)` ที่ใช้มา 3 บท ข้างในคือ: embed คำค้น → คำนวณ cosine กับโน้ตทุกตัว → เรียงคะแนน → คืน top-k
(ที่ว่า "เร็วมาก" ทำยังไงเมื่อโน้ตเป็นล้าน — บทที่ 6: ANN)

หมายเหตุ: Chroma รายงานเป็น **distance** (ระยะ) = ประมาณ `1 − similarity` — เลขน้อย = ใกล้
demo ของเราจึงพิมพ์ `1-dist` ให้อ่านเป็นคะแนนความใกล้

## 4.6 เกร็ดที่โปรรู้ (ย่อจาก deep-technical)

- ทำไมนิยม cosine ไม่ใช่ระยะทางตรงๆ (Euclidean)? — ตัดผลของ "ความยาวเอกสาร" ออก
  เหลือแต่ "ทิศ=ความหมาย" + เสถียรเชิงตัวเลขกว่า (Ch1, Ch50)
- embedder ยุคใหม่ปล่อยเวกเตอร์ normalize มาแล้ว (‖v‖=1) → cosine = dot product เฉยๆ
  → เร็วขึ้นอีก (Ch1 §1.5)

---

### สรุปบทที่ 4
- ความใกล้ของความหมาย = **cos(A,B) = A·B / (|A||B|)** — สมการเดียว, โค้ด 5 บรรทัด
- คำนวณมือได้ใน 2 มิติ · สเกลขึ้น 1024 มิติ ฟังก์ชันเดิมเป๊ะ (พิสูจน์แล้ว: แมว 0.82 vs น้ำมัน 0.31)
- vector DB = ที่เก็บเวกเตอร์ + หา cosine สูงสุดเร็วๆ — แค่นั้นจริงๆ

*โค้ด: `book/demo/demo4_cosine_by_hand.py` (รันพิสูจน์แล้ว) · ลึกกว่า: deep-technical Ch1/8/50*
